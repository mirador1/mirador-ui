stages:
  - validate
  - test
  - build
  - quality

variables:
  NODE_VERSION: "22"

default:
  image: node:${NODE_VERSION}-alpine
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
    policy: pull-push

# ── Install ──────────────────────────────────────────────────────────────────
.install:
  before_script:
    - npm ci --prefer-offline

# ── Stage: validate ─────────────────────────────────────────────────────────
typecheck:
  stage: validate
  extends: .install
  script:
    - npx tsc --noEmit -p tsconfig.app.json
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

lint:format:
  stage: validate
  extends: .install
  script:
    - npx prettier --check "src/**/*.{ts,html,scss,json}"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

# ── Stage: test ──────────────────────────────────────────────────────────────
unit-tests:
  stage: test
  extends: .install
  script:
    - npx ng test --watch=false
  coverage: '/All files\s*\|\s*[\d.]+/'
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

# ── Node 20 compat ──────────────────────────────────────────────────────────
unit-tests:node20:
  stage: test
  extends: .install
  image: node:20-alpine
  script:
    - npx ng test --watch=false
  rules:
    - if: $CI_COMMIT_BRANCH == "main"

# ── Stage: build ─────────────────────────────────────────────────────────────
build:production:
  stage: build
  extends: .install
  script:
    - |
      BUILD_OUT=$(npx ng build --configuration production 2>&1)
      echo "$BUILD_OUT"
      mkdir -p dist
      echo "$BUILD_OUT" > dist/build-warnings.txt
    - |
      echo "── Bundle analysis ──"
      du -sh dist/customer-observability-ui/browser/*.js 2>/dev/null | sort -rh | head -10
      echo ""
      TOTAL=$(du -sh dist/customer-observability-ui/browser/ 2>/dev/null | cut -f1)
      echo "Total bundle size: $TOTAL"
  artifacts:
    paths:
      - dist/
    expire_in: 7 days
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

lint:circular-deps:
  stage: build
  needs: ["build:production"]
  script:
    - |
      echo "Checking for circular imports in build output..."
      if grep -qi "circular" dist/build-warnings.txt 2>/dev/null; then
        echo "FAIL: Circular dependencies detected"
        grep -i "circular" dist/build-warnings.txt
        exit 1
      fi
      echo "OK: No circular dependencies"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

# ── Stage: quality ───────────────────────────────────────────────────────────
bundle-size-check:
  stage: quality
  needs: ["build:production"]
  script:
    - |
      MAX_SIZE_KB=500
      MAIN_JS=$(ls dist/customer-observability-ui/browser/main-*.js 2>/dev/null | head -1)
      if [ -z "$MAIN_JS" ]; then
        echo "WARN: No main-*.js found in dist, skipping bundle size check"
        exit 0
      fi
      INITIAL=$(du -sk "$MAIN_JS" | cut -f1)
      echo "Initial bundle: ${INITIAL}kB (max: ${MAX_SIZE_KB}kB)"
      if [ "$INITIAL" -gt "$MAX_SIZE_KB" ]; then
        echo "WARN: Initial bundle exceeds ${MAX_SIZE_KB}kB"
      else
        echo "OK: Bundle within budget"
      fi
  allow_failure: true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"

security:audit:
  stage: quality
  extends: .install
  script:
    - npm audit --audit-level=high || true
    - |
      echo "── Checking for sensitive files ──"
      SENSITIVE=$(find src -name ".env*" -o -name "*.pem" -o -name "*.key" -o -name "credentials*" 2>/dev/null)
      if [ -n "$SENSITIVE" ]; then
        echo "FAIL: Sensitive files found in src/:"
        echo "$SENSITIVE"
        exit 1
      fi
      echo "OK: No sensitive files in src/"
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == "main"
