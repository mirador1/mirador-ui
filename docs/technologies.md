# Technology glossary

> **TODO — placeholder.** This file is being populated with ~1000 entries.
> Each entry includes: (1) what it is, (2) how this project uses it, (3) why it's pertinent here.
> Work is tracked in `TASKS.md`.

The goal is a single exhaustive reference listing every piece of tech touched by this codebase
(Angular, TypeScript, Vitest, Kaniko, hadolint, Trivy, Prettier, GitLab CI, Docker buildx,
Keycloak, Loki, Tempo, Pyroscope, Prometheus, PostgreSQL, Kafka, Redis, Ollama, …) alongside the
equivalent glossary in the `mirador-service` repo, so a newcomer can answer "why is this
dependency in our stack?" without grep-ing the codebase.

Until the entries land, consult:

- [Architecture](architecture.md) for the runtime topology.
- [ADRs](adr/) for the rationale behind individual choices (zoneless, signals, raw SVG, Vitest, standalone components).
- [Tech stack summary](../README.md#tech-stack) in the root README for the short version.
